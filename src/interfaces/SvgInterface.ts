export namespace Svg {
  export interface SvgWaves{
    colorLight: string; 
    colorDark: string; 
  }
  
  export interface RippedPaperItems{
    id?: string;
    colorLight: string;
    colorDark: string;
}
}